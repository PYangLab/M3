% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{m3_augment}
\alias{m3_augment}
\title{Synthesize new donors per condition.}
\usage{
m3_augment(model, conditions, n_donors, tau = 0.8, batch = NULL, seed = 42L)
}
\arguments{
\item{model}{an \code{m3_model}.}

\item{conditions}{character vector of conditions to synthesize (in order).}

\item{n_donors}{integer vector, synthetic donors per condition (same length).}

\item{tau}{posterior temperature (default 0.8).}

\item{batch}{optional batch label to template from (needs \code{batch_key} at
training); \code{NULL} draws from all batches.}

\item{seed}{integer seed (default 42).}
}
\value{
a list with \code{expression} (named list of cells x features matrices,
  one per modality) and \code{obs} (a data.frame, one row per synthetic cell).
}
\description{
Resamples real donor templates per condition through the VAE posterior,
producing fresh synthetic donors with per-cell-type expression.
}
\examples{
\donttest{
  aug <- m3_augment(model, conditions = c("HC", "Severe"), n_donors = c(3, 3), tau = 0.8)
  dim(aug$expression$rna)
}
}
