% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attribute.R
\name{m3_top_genes}
\alias{m3_top_genes}
\title{Per-celltype-balanced top genes (the publication recipe).}
\usage{
m3_top_genes(
  attribution,
  n = 100L,
  min_cells_per_condition = 200L,
  exclude_regex,
  modality = NULL
)
}
\arguments{
\item{attribution}{an \code{m3_attribution} from \code{\link{m3_attribute}}.}

\item{n}{number of genes to return (default 100).}

\item{min_cells_per_condition}{cell-type filter threshold (default 200; 0 to skip).}

\item{exclude_regex}{regex of names to drop; \code{NULL} to skip; missing uses
the default housekeeping pattern.}

\item{modality}{"rna"/"adt"/"atac" to restrict ranking; \code{NULL} for all.}
}
\value{
a data.frame: \code{feature}, \code{modality}, \code{score}, \code{n_celltypes_used}.
}
\description{
Drops cell types with fewer than \code{min_cells_per_condition} cells in
either condition, scores each gene by \code{mean(|gene x celltype IG|)} over
the kept cell types, excludes housekeeping/ribosomal genes by name, and
optionally restricts to one modality.
}
