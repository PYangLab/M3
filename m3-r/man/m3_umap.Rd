% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{m3_umap}
\alias{m3_umap}
\title{UMAP projection identical to the m3 Python tutorials.}
\usage{
m3_umap(
  x,
  method = c("scanpy", "umap"),
  n_neighbors = 15L,
  min_dist = 0.1,
  spread = 1,
  metric = "euclidean",
  random_state = 0L,
  device = "auto"
)
}
\arguments{
\item{x}{a numeric matrix (rows = points), e.g. an \code{\link{m3_embedding}}.}

\item{method}{"scanpy" or "umap".}

\item{n_neighbors}{UMAP neighbours (default 15; capped at nrow-1).}

\item{min_dist, spread, metric}{umap-learn parameters (\code{method = "umap"}).}

\item{random_state}{umap-learn seed (default 0).}

\item{device}{session device (defaults to "auto"; pass the model's device).}
}
\value{
a 2-column numeric matrix of coordinates.
}
\description{
Runs scanpy's neighbors+UMAP (\code{method = "scanpy"}, as in Tutorial 1) or
\code{umap.UMAP} (\code{method = "umap"}, as in Tutorials 2 and 4) inside the
engine environment, so the coordinates match the Python tutorial exactly.
}
\examples{
\donttest{
  emb <- m3_embedding(model, "bio")
  xy  <- m3_umap(emb, method = "scanpy")
}
}
