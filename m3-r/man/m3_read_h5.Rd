% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{m3_read_h5}
\alias{m3_read_h5}
\title{Read one batch from paper-format HDF5 matrices + a metadata CSV.}
\usage{
m3_read_h5(
  rna = NULL,
  adt = NULL,
  atac = NULL,
  metadata,
  batch = "batch0",
  device = c("auto", "cpu", "cuda")
)
}
\arguments{
\item{rna, adt, atac}{paths to per-modality \code{.h5} files (adt/atac optional).}

\item{metadata}{path to the per-cell metadata \code{.csv}.}

\item{batch}{batch label written to \code{obs$batch}.}

\item{device}{session device (see \code{\link{m3_dataset}}).}
}
\value{
an \code{m3_dataset}.
}
\description{
Mirrors \code{m3.read_h5} in Python: each matrix is an HDF5 file with the
counts under \code{matrix/data} and names under \code{matrix/features}; the
metadata CSV provides the per-cell role columns. Big data stays in Python
(never materialised on the R heap).
}
\examples{
\donttest{
  d <- m3_read_h5(rna = "rna1.h5", adt = "adt1.h5",
                  metadata = "metadata1.csv", batch = "batch1")
}
}
